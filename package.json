{
  "name": "ishtihoss-profile",
  "private": true,
  "type": "module",
  "description": "Generator for the ishtihoss/ishtihoss profile README graphics",
  "scripts": {
    "build": "node tools/build-assets.mjs",
    "telemetry": "mkdir -p dist && node tools/fetch-telemetry.mjs > dist/telemetry.json && node tools/build-telemetry.mjs dist/telemetry.json dist/telemetry.svg"
  },
  "devDependencies": {
    "opentype.js": "^1.3.4"
  }
}
